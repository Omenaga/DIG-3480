using UnityEngine;

public class GithubVSTest : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // Test for editing in Unity and push to GitHub (JW)
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
